import { PackagePropertyString } from "../types";
/**
 * A Property String is an arbitrary string value to display to the user
 */
export declare class PropertyString {
    readonly data: PackagePropertyString;
    private constructor();
    /**
     * Returns a new Property String with the provided options
     * @param value
     * @returns
     */
    static of(value: Omit<PackagePropertyString, "type">): PropertyString;
}
