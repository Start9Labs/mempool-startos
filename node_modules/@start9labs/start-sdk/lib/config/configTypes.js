// sometimes the type checker needs just a little bit of help
export function isValueSpecListOf(t, s) {
    return "spec" in t && t.spec.type === s;
}
export const unionSelectKey = "unionSelectKey";
export const unionValueKey = "unionValueKey";
