export const dependenciesSet = () => ({
    exists(id) {
        return {
            id,
            kind: "exists",
        };
    },
    running(id) {
        return {
            id,
            kind: "running",
        };
    },
});
