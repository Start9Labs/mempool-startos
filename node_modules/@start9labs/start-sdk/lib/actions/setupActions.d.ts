import { ActionMetaData } from "../types";
import { CreatedAction } from "./createAction";
export declare function setupActions(...createdActions: CreatedAction<any, any>[]): {
    readonly actions: Record<string, CreatedAction<any, any, any>>;
    readonly actionsMetadata: ActionMetaData[];
};
