export function cooldownTrigger(timeMs) {
    return async function* () {
        while (true) {
            await new Promise((resolve) => setTimeout(resolve, timeMs));
            yield;
        }
    };
}
