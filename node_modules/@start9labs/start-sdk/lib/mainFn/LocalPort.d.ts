import { Effects } from "../types";
import { LocalBinding } from "./LocalBinding";
export declare class LocalPort {
    readonly effects: Effects;
    constructor(effects: Effects);
    static bindLan(effects: Effects, internalPort: number): Promise<LocalBinding>;
}
