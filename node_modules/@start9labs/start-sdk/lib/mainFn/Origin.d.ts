export declare class Origin {
    readonly protocol: string | null;
    readonly host: string;
    constructor(protocol: string | null, host: string);
    build({ basic, path, search }: BuildOptions): string;
}
type BuildOptions = {
    basic: {
        username: string;
        password: string | null;
    } | null;
    path: string;
    search: Record<string, string>;
};
export {};
