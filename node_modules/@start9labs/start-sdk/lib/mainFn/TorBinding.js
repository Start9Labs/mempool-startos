import { Origin } from "./Origin";
export class TorBinding {
    host;
    constructor(host) {
        this.host = host;
    }
    createOrigin(protocol) {
        return new Origin(protocol, this.host);
    }
}
