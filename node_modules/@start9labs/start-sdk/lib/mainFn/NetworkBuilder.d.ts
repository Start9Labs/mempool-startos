import { Effects } from "../types";
import { TorHostname } from "./TorHostname";
export declare class NetworkBuilder {
    private effects;
    static of(effects: Effects): NetworkBuilder;
    private constructor();
    getTorHostName(id: string): TorHostname;
}
