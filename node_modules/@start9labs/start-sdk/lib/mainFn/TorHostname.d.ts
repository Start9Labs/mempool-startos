import { Effects } from "../types";
import { TorBinding } from "./TorBinding";
export declare class TorHostname {
    readonly effects: Effects;
    readonly id: string;
    constructor(effects: Effects, id: string);
    static of(effects: Effects, id: string): TorHostname;
    bindTor(internalPort: number, externalPort: number): Promise<TorBinding>;
}
