import { SDKManifest } from "../manifest/ManifestTypes";
import { AutoConfig, AutoConfigFrom } from "./AutoConfig";
export declare function setupAutoConfig<WD, Input, Manifest extends SDKManifest, NestedConfigs extends {
    [key in keyof Manifest["dependencies"]]: unknown;
}>(configs: AutoConfigFrom<WD, Input, NestedConfigs>): AutoConfigFrom<WD, Input, NestedConfigs> extends infer T ? { [k in keyof T]: AutoConfig<WD, Input, NestedConfigs>; } : never;
