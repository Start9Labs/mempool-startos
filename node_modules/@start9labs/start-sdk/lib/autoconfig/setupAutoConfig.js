import { AutoConfig } from "./AutoConfig";
export function setupAutoConfig(configs) {
    const answer = { ...configs };
    for (const key in configs) {
        answer[key] = new AutoConfig(configs, key);
    }
    return answer;
}
