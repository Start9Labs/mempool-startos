import { Backups } from "./Backups";
import { SDKManifest } from "../manifest/ManifestTypes";
import { ExpectedExports } from "../types";
import { _ } from "../util";
export type SetupBackupsParams<M extends SDKManifest> = Array<(keyof M["volumes"] & string) | Backups<M>>;
export declare function setupBackups<M extends SDKManifest>(...args: _<SetupBackupsParams<M>>): {
    readonly createBackup: ExpectedExports.createBackup;
    readonly restoreBackup: ExpectedExports.restoreBackup;
};
