export class Migration {
    options;
    constructor(options) {
        this.options = options;
    }
    static of(options) {
        return new Migration(options);
    }
    async up(opts) {
        this.up(opts);
    }
    async down(opts) {
        this.down(opts);
    }
}
