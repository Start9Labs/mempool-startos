export function setupInit(migrations, install, uninstall) {
    return {
        init: async (opts) => {
            await migrations.init(opts);
            await install.init(opts);
        },
        uninit: async (opts) => {
            await migrations.uninit(opts);
            await uninstall.uninit(opts);
        },
    };
}
