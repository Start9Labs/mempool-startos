import { sdk } from '../../sdk'

/**
 * Add each new Action as the next argument to this function
 *
 * Hello Moon has no Actions. See Hello World for an example
 */
export const { actions, actionsMetadata } = sdk.setupActions()
